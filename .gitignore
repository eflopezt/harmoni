# Snapshots de conceptos (generados por manage.py snapshot_conceptos)
snapshots/

# Provisioning de clientes Enterprise (CONTIENE SECRETS — nunca commitear)
provisioning/

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg

# Virtual Environment
venv/
env/
ENV/
.venv

# Django
*.log
logs/
local_settings.py
db.sqlite3
db.sqlite3-journal
media/
staticfiles/
*.pot

# Environment variables & secrets
.env
.env.local
.env.*.local
# Archivo real de prod/demo: NUNCA trackear (usar deploy/.env.production.template como base).
# Lo lee el compose vía `env_file: .env.production` relativo a deploy/.
.env.production
deploy/.env.production
.secrets
deploy/.secrets
claves.md
**/claves.md

# IDEs
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store

# Testing
.coverage
htmlcov/
.pytest_cache/
.tox/
*.cover

# Celery
celerybeat-schedule
celerybeat.pid

# Docker
*.log

# Misc
*.bak
*.tmp
*.backup
backups/

# Runtime
.server_pid

# Cotizaciones / credenciales (NUNCA committear — datos cliente o sensibles)
Cotizacion_*.pdf
Cotizacion_*.xlsx
credenciales_*.xlsx
credenciales_*.csv

# Scripts one-off de exploración / fixes en raíz (no son parte del producto)
/analyze_*.py
/capture_*.py
/crear_excel_*.py
/create_excel_*.py
/extract_*.py
/fix_*.py
/import_*.py
/populate_*.py
/rebuild_*.py

# Capturas fuente para regenerar el manual (la salida .pdf/.docx sí se versiona)
docs/manual_capturas/
.claude/scheduled_tasks.lock

# Artefactos de pruebas locales (PDFs descargados como evidencia)
/.reporte_*.pdf
/.boleta_*.pdf
/.constancia_*.pdf

# Bot WhatsApp standalone (proyecto aparte, no parte del ERP)
/harmoni-wa-bot/

# Logos en raíz (vivir en static/img/ si se versionan)
/LOGO HARMONI*.png

# Artefactos de agentes/auditorías/crawls (no versionar — un git add -A los arrastraba)
.claude/worktrees/
.claude/settings.local.json
/errores_import.xlsx
/scripts/_crawl_result.txt
